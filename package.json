{
  "name": "h-ui",
  "version": "1.0.0",
  "description": "氢 ui库",
  "author": "Yin-Jinlong",
  "type": "module",
  "scripts": {
    "packages:build": "pnpm -C packages build",
    "packages:test": "pnpm -C packages test",
    "docs:dev": "pnpm -C docs dev",
    "docs:build": "pnpm -C docs build"
  },
  "devDependencies": {
    "@yin-jinlong/h-ui-build-tools": "link:./build/dist"
  },
  "license": "LGPL-3.0-or-later"
}
