{
  "name": "h-ui",
  "version": "1.0.0",
  "description": "氢 ui库",
  "author": "Yin-Jinlong",
  "type": "module",
  "scripts": {
    "packages:build": "pnpm -C packages build",
    "packages:test": "pnpm -C packages test",
    "docs:dev": "pnpm -C docs dev",
    "docs:build": "pnpm -C docs build"
  },
  "devDependencies": {
    "@yin-jinlong/build": "^0.0.1",
    "@yin-jinlong/run": "^0.0.1",
    "tsx": "^4.7.2"
  },
  "license": "LGPL-3.0-or-later"
}
